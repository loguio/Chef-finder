{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('home') }}
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block title %}Accueil{% endblock %}

{% block body %}

<div class="app">
    <nav class="position-absolute w-100" id="navbar">
        <div class="d-flex justify-content-between mx-5">
            <div class="d-flex align-items-end">
                <div id="logo" class="navbar-brand mr-5">
                    <img src="{{ asset('build/images/logo.svg') }}" alt="Logo ChieFinder" style="width: 60px;"/>
                </div>
                <div class="nav-item mx-2 mb-2 text-center">
                    <a class="nav-link font-weight-bold pb-0" href="{{ path('app_orders') }}">Réservation</a>
                    <i class="fas fa-circle text-center menuItemDot"></i>
                </div>
                <div class="nav-item mx-2 mb-2 text-center">
                    <a class="nav-link font-weight-bold pb-0" href="{{ path('app_chief_list') }}">Les chefs</a>
                    <i class="fas fa-circle text-center menuItemDot"></i>
                </div>
                <div class="nav-item mx-2 mb-2 text-center">
                    <a class="nav-link font-weight-bold pb-0" href="{{ path('app_about_us') }}">Qui sommes-nous ?</a>
                    <i class="fas fa-circle text-center menuItemDot"></i>
                </div>
                <div class="nav-item mx-2 mb-2 text-center">
                    <a class="nav-link font-weight-bold pb-0" href="{{ path('app_contact') }}">Contact</a>
                    <i class="fas fa-circle text-center menuItemDot"></i>
                </div>
            </div>
            <div class="d-flex align-items-center">
                <a class="nav-item mr-4" href="{{ path('app_cart') }}">
                    <i class="fas fa-shopping-bag fa-2x text-muted"></i>
                </a>
                <a class="btn text-light my-auto px-4" id="NavLoginBtn" href="{{ path('app_customer_login') }}">Connexion</a>
            </div>
        </div>
    </nav>
    <div class="vh-100 swiper mySwiper">
        <div class="swiper-wrapper">
            <div class="swiper-slide bg-success">Slide 1</div>
            <div class="swiper-slide bg-warning">Slide 2</div>
            <div class="swiper-slide bg-secondary">Slide 3</div>
            <div class="swiper-slide bg-success">Slide 4</div>
            <div class="swiper-slide bg-info">Slide 5</div>
            <div class="swiper-slide bg-warning">Slide 6</div>
            <div class="swiper-slide bg-success">Slide 7</div>
            <div class="swiper-slide bg-success">Slide 8</div>
            <div class="swiper-slide bg-success">Slide 9</div>
        </div>
        <div class="swiper-pagination mr-3"></div>
    </div>
</div>

{% endblock %}
